{
  "name": "exampro-monorepo",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "dev": "concurrently \"npm run dev:backend\" \"npm run dev:frontend\"",
    "dev:frontend": "npm run dev --workspace=exampro-frontend",
    "dev:backend": "npm run dev --workspace=exampro-backend",
    "build": "npm run build --workspace=exampro-frontend && npm run build --workspace=exampro-backend",
    "install:all": "npm install",
    "clean": "npm run clean --workspace=exampro-frontend && npm run clean --workspace=exampro-backend && npm run clean --workspace=exampro-shared"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  },
  "dependencies": {
    "@google/genai": "^1.29.0"
  }
}
